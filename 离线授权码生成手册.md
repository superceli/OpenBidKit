# 离线授权码生成手册

## 前提

- Node.js >= 20
- 私钥文件：`client/scripts/license-private-key.json`（首次已生成，**绝对不要提交 git，丢失后需要重新生成密钥对并替换客户端公钥**）

## 快速命令

先 `cd client` 进入目录，然后二选一：

```bash
# 1. 一码通用（不绑定任何设备，任何电脑都能激活）
node scripts/generate-offline-license.cjs --days 30

# 2. 一机一码（只绑定指定设备）
node scripts/generate-offline-license.cjs --days 365 --machine-fp 用户的fingerprint
```

## 完整参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--days` | 授权有效天数 | `30` |
| `--machine-fp` | 绑定的设备 fingerprint（空=一码通用） | 空 |
| `--client-id` | 绑定的 clientId（空=通配 *） | `*` |
| `--features` | 逗号分隔的功能列表 | `basic,green-report` |
| `--version-min` | 最低兼容版本 | `1.0.0` |
| `--version-max` | 最高兼容版本 | `99.99.99` |
| `--private-key` | 私钥文件路径 | 脚本同目录 `license-private-key.json` |
| `--full` | 额外输出完整 JSON 信封 | 关闭 |

## 常用场景

```bash
# —— 测试 / 给几个熟人，一码通用 ——
node scripts/generate-offline-license.cjs --days 7          # 7 天
node scripts/generate-offline-license.cjs --days 30         # 30 天
node scripts/generate-offline-license.cjs --days 3650       # 10 年（近似永久）

# —— 正式发给付费用户，一机一码 ——
node scripts/generate-offline-license.cjs --days 365 --machine-fp a1b2c3d4e5f6...

# —— 限制版本范围（比如只给 1.x 用户）——
node scripts/generate-offline-license.cjs --days 365 --version-max 1.99.99

# —— 自定义功能权限 ——
node scripts/generate-offline-license.cjs --days 30 --features basic,green-report,plugin
```

## 怎么拿到用户的 fingerprint

用户需要在他电脑上跑：

```bash
# 方法 A：让用户打开软件的 DevTools（F12），在 Console 里执行：
require('./electron/utils/machineIdentity.cjs').getMachineFingerprintHash()

# 方法 B：命令行直接跑（需要先进入 client 目录）：
node -e "const m=require('./electron/utils/machineIdentity.cjs');console.log(m.getMachineFingerprintHash())"
```

拿到一串 hash（类似 `a1b2c3d4e5f6...`），塞到 `--machine-fp` 里再生成即可。

## 输出示例

```
╔══════════════════════════════════════════════════════════╗
║           哲元绿证报告工具箱 · 离线授权码                      ║
╠══════════════════════════════════════════════════════════╣
║
║  有效期  ：30 天（至 2026-10-10）
║  设备绑定：否（一码通用）
║  功能    ：basic, green-report
║  版本范围：1.0.0 ~ 99.99.99
║
║  授权码  ：
║
║  YB-LICENSE-eyJwYXlsb2FkIjp7...
║
╚══════════════════════════════════════════════════════════╝
```

把 `YB-LICENSE-` 开头那整串复制发给用户，他在软件激活页粘贴即可。

## 重新生成密钥对（万不得已才用）

如果私钥丢了或泄漏了，需要换一对：

```bash
cd client
node -e "
const { webcrypto } = require('node:crypto');
const fs = require('fs');
(async () => {
  const kp = await webcrypto.subtle.generateKey({ name: 'ECDSA', namedCurve: 'P-256' }, true, ['sign', 'verify']);
  const pub = await webcrypto.subtle.exportKey('jwk', kp.publicKey);
  const priv = await webcrypto.subtle.exportKey('jwk', kp.privateKey);
  fs.writeFileSync('scripts/license-private-key.json', JSON.stringify(priv, null, 2));
  fs.writeFileSync('electron/resources/license-public-key.json', JSON.stringify(pub, null, 2));
  console.log('✅ 密钥对已重新生成');
})();
"
```

⚠️ 换完密钥对，**之前发出去的所有授权码全部失效**，需要重新给所有用户发新码。
